#!/bin/bash
xhost local:root
[ -d "$HOME/workspace" ] || mkdir "$HOME/workspace"
exec docker run -i --net=host --rm -e DISPLAY="$DISPLAY" -v "$HOME/workspace/:/workspace/:z" docbill/ubuntu-umake-eclipse:14.04 "$@"

